{
  "name": "renter-passport",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Renter Passport — mobile-first tenant application + lease app (React, deploys as its own Netlify site).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "box-node-sdk": "^3.6.0",
    "busboy": "^1.6.0",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/qrcode": "^1.5.5",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  },
  "comments": {
    "functions": "box-node-sdk + busboy are server-only (used by netlify/functions); never imported by src, so they don't enter the client bundle. box-node-sdk is marked external in netlify.toml."
  }
}
